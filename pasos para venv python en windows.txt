- Pasos para iniciar virtual environment de python en Windows

python -m venv .venv
.\.venv\Scripts\activate
pip install pandas
pip install scikit-learn
pip install holidays
pip install skforecast
pip install "statsmodels>=0.12, <0.15
pip install matplotlib
pip install plotly

pip install seaborn
pip install skforecast[sarimax]
pip install shap


PARA STREAMLIT:
pip install streamlit
streamlit run app.py